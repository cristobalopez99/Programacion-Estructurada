#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numOne = 8;
    int numTwo = 10;

    printf("The value in One is: %i \n", numOne);
    printf("The value in two is: %i \n", numTwo);

    printf("The interchanged value of one is: %i \n", numTwo);
    printf("The interchanged value of two is: %i \n", numOne);

    return 0;
}
